/**
 * @elevenlabs/types
 *
 * TypeScript types for ElevenLabs Agent API communication
 * Auto-generated from AsyncAPI specification
 */

// Re-export incoming message types (messages the client receives from the server)
export * as Incoming from "../generated/types/incoming";

// Re-export outgoing message types (messages the client sends to the server)
export * as Outgoing from "../generated/types/outgoing";

// Export all types for cases where you need direct access
export * from "../generated/types/asyncapi-types";
